name: TitanCore Production Monitor

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Check backend API
        run: |
          curl --fail --silent --show-error \
            http://178.105.30.16:5000/api/projects

      - name: Check frontend
        run: |
          curl --fail --silent --show-error \
            http://178.105.30.16:3000

      - name: Monitoring successful
        run: echo "TitanCore production services are healthy."